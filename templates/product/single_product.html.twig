<div class="product-item text-center">
	<a href="{{ path('product', {'id' : product.id}) }}"><img src="/uploads/products/{{ product.illustration }}" alt="{{ product.name }}" class="w-100 imageProduct" ></a>
	<h5>{{ product.name }}</h5>
	<span class="product-subtittle">{{ product.subtittle }}</span>
	<span class="product-price">{{ (product.price / 100) | number_format(2, ',', '.')}}
		€</span>
</div>
