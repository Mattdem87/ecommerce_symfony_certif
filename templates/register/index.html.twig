{% extends 'base.html.twig' %}

{% block content %}
	<div class="row">
		<div class="col-8 m-auto">
			<h1>Inscription</h1>

			{% if notification %}

				<div class="alert alert-info">{{ notification }}</div>
				<hr>
			{% endif %}

			{{ form(form) }}

			</div>
		</div>

	{% endblock %}
